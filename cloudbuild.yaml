steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - compute
  - ssh
  - sugi-instance
  - --zone=us-central1-a
  - --command="cd my-cicd-app && git pull && npm install && pm2 restart all"

options:
  logging: CLOUD_LOGGING_ONLY
